spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          routes: []
management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info,gateway
  endpoint:
    health:
      show-details: always
      
server:
  port: 8080
